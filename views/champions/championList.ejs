<!-- Heading content -->
<% if (title) { %>
    <h1 id="ChampTitleMain"><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<!-- Message Block -->
<%# messages() %>

<!-- Grid -->
<%- grid %>